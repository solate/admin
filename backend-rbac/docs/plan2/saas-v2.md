# SaaS多租户权限系统产品设计文档（完整版）

> 基于若依最佳实践 + Casbin + JWT 的企业级权限系统完整产品设计
>
> 版本：v2.0 Complete
> 日期：2026-01-27
> 文档类型：产品设计文档（纯产品设计，无代码实现）
> 参考标准：若依框架、RBAC模型、业界SaaS最佳实践

---

## 📋 文档导航

1. [产品概述](#一产品概述)
2. [核心功能模块](#二核心功能模块)
3. [权限控制体系](#三权限控制体系)
4. [认证与授权机制](#四认证与授权机制)
5. [数据权限设计](#五数据权限设计)
6. [前端展示方案](#六前端展示方案)
7. [后台管理功能](#七后台管理功能)
8. [用户操作流程](#八用户操作流程)
9. [典型应用场景](#九典型应用场景)
10. [安全机制设计](#十安全机制设计)
11. [产品交互细节](#十一产品交互细节)
12. [扩展功能规划](#十二扩展功能规划)

---

## 一、产品概述

### 1.1 产品定位

本产品是一个面向中大型企业的**多租户SaaS内容管理平台**，采用业界成熟的RBAC（基于角色的访问控制）模型，提供企业级的精细化权限管理能力。

**核心价值主张：**

- **多租户隔离**：租户之间数据和权限完全隔离，确保数据安全
- **精细化权限**：四级权限控制（菜单、按钮、API、数据），满足复杂场景
- **灵活授权**：通过角色聚合权限，支持角色继承和权限模板
- **安全可靠**：多重安全机制，确保系统和数据安全
- **易于扩展**：统一权限管理，便于功能扩展和维护

### 1.2 适用场景

| 场景类型 | 说明 | 典型客户 |
|---------|------|---------|
| **企业内部管理系统** | 中大型企业内部的OA、ERP、CRM系统 | 制造业、零售业 |
| **SaaS多租户平台** | 为多个客户提供服务的SaaS平台 | SaaS服务商 |
| **多层级组织架构** | 集团公司、连锁企业的跨部门管理 | 集团企业、连锁机构 |
| **精细化权限控制** | 需要严格权限控制的金融、医疗行业 | 银行、医院 |

### 1.3 技术选型说明

**后端技术栈：**
- **语言**：Go 1.21+
- **Web框架**：Gin（高性能HTTP框架）
- **ORM**：GORM（Go语言最流行的ORM）
- **认证**：JWT（JSON Web Token）
- **权限**：Casbin（强大的访问控制库）
- **数据库**：PostgreSQL（支持JSON、递归查询）
- **缓存**：Redis（高性能缓存）

**前端技术栈（参考）：**
- **框架**：Vue 3 / React 18
- **UI组件库**：Element Plus / Ant Design
- **状态管理**：Pinia / Redux Toolkit
- **路由管理**：Vue Router / React Router
- **HTTP客户端**：Axios

### 1.4 核心概念

在深入产品设计前，需要理解以下核心概念：

| 概念 | 定义 | 示例 |
|-----|------|------|
| **租户（Tenant）** | SaaS系统的独立客户，拥有独立的用户和权限 | 企业A、企业B |
| **用户（User）** | 系统的使用者，属于某个租户 | 张三、李四 |
| **角色（Role）** | 权限的集合，用户通过角色获得权限 | 管理员、普通员工 |
| **权限（Permission）** | 对系统资源的访问许可 | 查看用户、新增用户 |
| **菜单权限（Menu）** | 控制前端菜单显示的权限 | 系统管理 > 用户管理 |
| **按钮权限（Button）** | 控制页面按钮显示的权限 | 新增、编辑、删除按钮 |
| **API权限（API）** | 控制后端接口访问的权限 | GET /api/v1/users |
| **数据权限（Data Scope）** | 控制数据访问范围的权限 | 仅本人数据、本部门数据 |

---

## 二、核心功能模块

### 2.1 功能模块总览

```
SaaS权限管理系统
│
├── 1. 用户管理模块
│   ├── 用户列表查询
│   ├── 用户新增/编辑/删除
│   ├── 用户角色分配
│   ├── 用户密码重置
│   ├── 用户状态管理
│   └── 用户导入导出
│
├── 2. 角色管理模块
│   ├── 角色列表查询
│   ├── 角色新增/编辑/删除
│   ├── 角色权限分配
│   ├── 角色数据权限配置
│   ├── 角色继承管理
│   └── 角色复制
│
├── 3. 权限管理模块
│   ├── 权限列表查询（树形）
│   ├── 权限新增/编辑/删除
│   ├── 权限标识管理
│   ├── API路径映射
│   └── 权限导入导出
│
├── 4. 菜单管理模块
│   ├── 菜单树维护
│   ├── 菜单新增/编辑/删除
│   ├── 菜单图标管理
│   ├── 菜单排序（拖拽）
│   └── 外链菜单管理
│
├── 5. 部门管理模块
│   ├── 部门树维护
│   ├── 部门新增/编辑/删除
│   ├── 部门排序（拖拽）
│   └── 部门人员管理
│
├── 6. 租户管理模块
│   ├── 租户列表查询
│   ├── 租户新增/编辑/删除
│   ├── 租户配额管理
│   ├── 租户数据隔离
│   └── 租户状态管理
│
├── 7. 审计日志模块
│   ├── 登录日志
│   ├── 操作日志
│   ├── 数据变更日志
│   └── 日志查询导出
│
└── 8. 个人中心模块
    ├── 个人信息维护
    ├── 修改密码
    ├── 登录历史
    └── 会话管理
```

### 2.2 用户管理模块详细设计

#### 2.2.1 功能清单

| 功能点 | 功能描述 | 优先级 | 权限标识 |
|-------|---------|--------|---------|
| **用户列表** | 分页展示用户信息，支持多条件搜索 | P0 | system:user:list |
| **用户新增** | 创建新用户，分配角色和部门 | P0 | system:user:add |
| **用户编辑** | 修改用户信息、角色、状态 | P0 | system:user:edit |
| **用户删除** | 软删除用户，检查关联数据 | P0 | system:user:delete |
| **密码重置** | 管理员强制重置用户密码 | P0 | system:user:resetPwd |
| **用户导入** | Excel批量导入用户 | P1 | system:user:import |
| **用户导出** | 导出用户列表为Excel | P1 | system:user:export |
| **启用/禁用** | 控制用户登录状态 | P1 | system:user:edit |
| **权限查看** | 查看用户的所有权限 | P2 | system:user:query |

#### 2.2.2 用户列表页面设计

**页面布局：**

```
┌─────────────────────────────────────────────────────────┐
│  系统管理 > 用户管理                                      │
├─────────────────────────────────────────────────────────┤
│  搜索条件：                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │用户名    │ │姓名      │ │手机号    │ │部门      │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │角色      │ │状态      │ │[ 查询 ]  │              │
│  └──────────┘ └──────────┘ └──────────┘              │
│                                                         │
│  操作按钮：                                               │
│  [ + 新增用户 ] [ 批量导入 ] [ 导出 ] [ 批量删除 ]      │
│                                                         │
│  用户列表：                                               │
│  ┌───────────────────────────────────────────────────┐ │
│  │ ☑  用户名    姓名    手机号   部门    角色    状态  │ │
│  │ ☑  张三      张三    138...  技术部   管理员  启用  │ │
│  │     └─ 操作：[编辑] [删除] [重置密码] [查看权限]   │ │
│  │ ☑  李四      李四    139...  产品部   普通员工 启用  │ │
│  │     └─ 操作：[编辑] [删除] [重置密码] [查看权限]   │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  分页：                                                  │
│  共 100 条  [< 1 2 3 4 5 >]  每页 [20] 条               │
└─────────────────────────────────────────────────────────┘
```

**表格列说明：**

| 列名 | 数据来源 | 说明 |
|-----|---------|------|
| 选择框 | - | 支持全选、单选 |
| 用户名 | users.username | 登录用户名，唯一标识 |
| 姓名 | users.name | 真实姓名 |
| 手机号 | users.phone | 联系电话（脱敏显示：138****1234） |
| 部门 | departments.name | 所属部门 |
| 角色 | roles.name | 逗号分隔多个角色 |
| 状态 | users.status | 启用/禁用（徽章样式） |
| 最后登录 | users.last_login_at | 相对时间（2小时前） |
| 操作 | - | 操作按钮（根据权限显示） |

#### 2.2.3 用户新增/编辑表单设计

**表单字段：**

| 字段名称 | 字段类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| 用户名 | 文本输入 | 是 | 4-20位，字母数字下划线 | - | 登录用户名，全局唯一 |
| 密码 | 密码输入 | 新增必填 | 6-20位，包含字母和数字 | 123456 | 编辑时可选择是否修改 |
| 确认密码 | 密码输入 | 新增必填 | 与密码一致 | - | 二次确认 |
| 姓名 | 文本输入 | 是 | 2-20位字符 | - | 真实姓名 |
| 手机号 | 手机输入 | 是 | 11位手机号 | - | 用于找回密码 |
| 邮箱 | 邮箱输入 | 否 | 标准邮箱格式 | - | 邮箱地址 |
| 性别 | 单选 | 否 | 男/女/保密 | 保密 | 性别信息 |
| 部门 | 树形选择 | 是 | 必须选择 | - | 所属部门（组织架构树） |
| 角色 | 多选框 | 是 | 至少选择一个 | - | 可选择多个角色 |
| 岗位 | 文本输入 | 否 | 最多50字符 | - | 职位名称 |
| 工号 | 文本输入 | 否 | 最多20字符 | - | 员工工号 |
| 状态 | 开关 | 是 | 启用/禁用 | 启用 | 用户状态 |
| 备注 | 文本域 | 否 | 最多200字符 | - | 备注信息 |

**表单交互逻辑：**

1. **用户名实时验证**：输入用户名后，失焦时异步检查是否已存在
2. **密码强度提示**：输入密码时，实时显示密码强度（弱/中/强）
3. **部门选择**：点击弹出部门树，支持搜索和展开/折叠
4. **角色多选**：显示所有可用角色，支持全选和搜索
5. **保存前验证**：点击保存时，先进行前端验证，再提交后端

#### 2.2.4 用户权限查看弹窗

**弹窗设计：**

```
┌─────────────────────────────────────────┐
│  用户权限详情 - 张三                     │
├─────────────────────────────────────────┤
│  基本信息：                               │
│  - 用户名：zhangsan                       │
│  - 姓名：张三                             │
│  - 部门：技术部 > 研发组                  │
│                                         │
│  拥有角色（2个）：                         │
│  ☑ 管理员                                │
│  ☑ 技术经理                              │
│                                         │
│  菜单权限（8个）：                         │
│  ☑ 系统管理                              │
│    ☑ 用户管理                            │
│    ☑ 角色管理                            │
│    ☑ 菜单管理                            │
│                                         │
│  按钮权限（15个）：                        │
│  ☑ system:user:add                       │
│  ☑ system:user:edit                      │
│  ☑ system:user:delete                    │
│  ...                                     │
│                                         │
│  API权限（25个）：                         │
│  ☑ GET /api/v1/users                     │
│  ☑ POST /api/v1/users                    │
│  ☑ PUT /api/v1/users/:id                 │
│  ...                                     │
│                                         │
│  数据权限：                               │
│  范围：本部门及以下数据                    │
│  部门：技术部及其所有子部门                │
│                                         │
│                  [ 关闭 ]                 │
└─────────────────────────────────────────┘
```

### 2.3 角色管理模块详细设计

#### 2.3.1 功能清单

| 功能点 | 功能描述 | 优先级 | 权限标识 |
|-------|---------|--------|---------|
| **角色列表** | 展示角色信息，支持搜索 | P0 | system:role:list |
| **角色新增** | 创建新角色，配置权限 | P0 | system:role:add |
| **角色编辑** | 修改角色信息和权限 | P0 | system:role:edit |
| **角色删除** | 删除角色（检查用户关联） | P0 | system:role:delete |
| **权限配置** | 为角色分配菜单和按钮权限 | P0 | system:role:permission |
| **数据权限配置** | 配置角色的数据范围 | P0 | system:role:dataScope |
| **角色复制** | 快速创建相似角色 | P1 | system:role:add |
| **角色继承** | 设置角色的父角色 | P2 | system:role:inherit |

#### 2.3.2 角色列表页面设计

**页面布局：**

```
┌─────────────────────────────────────────────────────────┐
│  系统管理 > 角色管理                                      │
├─────────────────────────────────────────────────────────┤
│  搜索条件：                                               │
│  ┌──────────┐ ┌──────────┐                              │
│  │角色名称  │ │状态      │ [ 查询 ]                    │
│  └──────────┘ └──────────┘                              │
│                                                         │
│  操作按钮：                                               │
│  [ + 新增角色 ]                                         │
│                                                         │
│  角色列表：                                               │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 角色名称     角色编码   用户数   数据权限   状态   │ │
│  │ 超级管理员   super_admin  1    全部数据    启用    │ │
│  │ └─ 操作：[编辑] [删除] [权限配置] [数据权限]        │ │
│  │ 租户管理员   admin       5    全部数据    启用    │ │
│  │ └─ 操作：[编辑] [删除] [权限配置] [数据权限]        │ │
│  │ 部门经理     dept_mgr    12   本部门及以下  启用    │ │
│  │ └─ 操作：[编辑] [删除] [权限配置] [数据权限]        │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

#### 2.3.3 角色权限配置界面设计

**界面布局：**

```
┌─────────────────────────────────────────────────────────┐
│  配置角色权限 - 租户管理员                                │
├─────────────────────────────────────────────────────────┤
│  左侧：权限树                           右侧：已选权限   │
│  ┌───────────────────────┐  ┌───────────────────────┐ │
│  │ 🔍 搜索权限...         │  │ 已选权限统计          │ │
│  │                       │  │ - 菜单：8个           │ │
│  │ [展开全部] [折叠全部]  │  │ - 按钮：15个          │ │
│  │                       │  │ - API：25个           │ │
│  │ ☑ 系统管理            │  │                       │ │
│  │   ├─ ☑ 用户管理       │  │ 已选权限列表          │ │
│  │   │   ├─ ☑ 用户查询   │  │ 📋 系统管理           │ │
│  │   │   ├─ ☑ 新增用户   │  │   └─ 👥 用户管理      │ │
│  │   │   ├─ ☑ 编辑用户   │  │       ├─ ➕ 新增用户  │ │
│  │   │   ├─ ☑ 删除用户   │  │       ├─ ✏️ 编辑用户  │ │
│  │   │   └─ ☐ 导出用户   │  │       └─ 🗑️ 删除用户  │ │
│  │   ├─ ☑ 角色管理       │  │                       │ │
│  │   │   ├─ ☑ 角色查询   │  │ 📋 业务管理           │ │
│  │   │   └─ ☐ 角色新增   │  │   └─ 📝 内容管理      │ │
│  │   └─ ☐ 菜单管理       │  │                       │ │
│  │ ☐ 业务管理            │  └───────────────────────┘ │
│  │   └─ ☐ 内容管理       │                          │
│  │                       │                          │
│  │ [全选] [取消全选]      │                          │
│  └───────────────────────┘                          │
│                                                         │
│  关联的API权限（自动生成）：                              │
│  ☑ GET /api/v1/users                                   │
│  ☑ POST /api/v1/users                                  │
│  ☑ PUT /api/v1/users/:id                               │
│  ☑ DELETE /api/v1/users/:id                            │
│                                                         │
│  [取消]  [保存]                                         │
└─────────────────────────────────────────────────────────┘
```

**交互说明：**

1. **左侧权限树**：
   - 支持勾选/取消勾选菜单和按钮
   - 父节点选中自动选中所有子节点
   - 支持搜索快速定位权限
   - 支持展开/折叠全部

2. **右侧已选权限**：
   - 实时显示已选权限数量
   - 按模块分组展示已选权限
   - 显示权限图标和名称

3. **API权限自动关联**：
   - 勾选按钮权限时，自动关联该按钮的API权限
   - 在底部显示已关联的API权限列表
   - 支持查看API详情（路径、方法、说明）

#### 2.3.4 角色数据权限配置界面

**界面设计：**

```
┌─────────────────────────────────────────────────────────┐
│  配置角色数据权限 - 部门经理                              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ○ 全部数据权限                                          │
│    💡 可查看所有数据，无额外限制                          │
│                                                         │
│  ○ 自定义数据权限                                        │
│    💡 可查看指定部门的数据                                │
│    ┌─────────────────────────────────────────┐         │
│    │ 选择部门：                                │         │
│    │                                          │         │
│    │ 🏢 技术部                                 │         │
│    │   ├─ ☑ 研发组                            │         │
│    │   └─ ☑ 测试组                            │         │
│    │ 🏢 产品部                                 │         │
│    │   └─ ☐ 产品组                            │         │
│    │ 🏢 市场部                                 │         │
│    │   └─ ☐ 市场组                            │         │
│    │                                          │         │
│    │ [展开全部] [折叠全部] [清空选择]          │         │
│    └─────────────────────────────────────────┘         │
│                                                         │
│  ● 本部门数据权限                                        │
│    💡 只能查看本部门的数据                                │
│                                                         │
│  ○ 本部门及以下数据权限                                  │
│    💡 可查看本部门及所有子部门的数据                      │
│                                                         │
│  ○ 仅本人数据权限                                        │
│    💡 只能查看本人创建的数据                              │
│                                                         │
│  [取消]  [保存]                                          │
└─────────────────────────────────────────────────────────┘
```

**数据权限说明：**

| 数据权限 | SQL条件示例 | 适用角色 | 说明 |
|---------|------------|---------|------|
| 全部数据权限 | 无额外WHERE | 超级管理员、租户管理员 | 可以查看所有数据 |
| 自定义数据权限 | `WHERE dept_id IN ('dept_001', 'dept_002')` | 跨部门管理者 | 可以查看指定部门的数据 |
| 本部门数据权限 | `WHERE dept_id = 'dept_001'` | 部门主管 | 只能查看本部门数据 |
| 本部门及以下 | `WHERE dept_id IN (递归子部门)` | 事业群负责人 | 可以查看本部门及子部门数据 |
| 仅本人数据权限 | `WHERE creator_id = 'user_001'` | 普通员工 | 只能查看自己创建的数据 |

### 2.4 权限管理模块详细设计

#### 2.4.1 功能清单

| 功能点 | 功能描述 | 优先级 | 权限标识 |
|-------|---------|--------|---------|
| **权限列表** | 树形展示所有权限 | P0 | system:permission:list |
| **权限新增** | 添加新权限（菜单/按钮/API） | P0 | system:permission:add |
| **权限编辑** | 修改权限信息和API关联 | P0 | system:permission:edit |
| **权限删除** | 删除权限（检查使用情况） | P0 | system:permission:delete |
| **权限查看** | 查看权限详情和关联API | P0 | system:permission:query |
| **权限导入** | 从Excel/JSON导入权限 | P1 | system:permission:import |
| **权限导出** | 导出权限为Excel/JSON | P1 | system:permission:export |
| **API关联管理** | 为按钮/菜单关联API | P0 | system:permission:api |

#### 2.4.2 权限列表界面设计

**界面布局：**

```
┌─────────────────────────────────────────────────────────┐
│  系统管理 > 权限管理                                      │
├─────────────────────────────────────────────────────────┤
│  筛选条件：                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │权限类型  │ │状态      │ │[ 查询 ]  │              │
│  └──────────┘ └──────────┘ └──────────┘              │
│                                                         │
│  操作按钮：                                               │
│  [ + 新增权限 ] [ 批量导入 ] [ 导出 ]                    │
│                                                         │
│  权限树：                                                 │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 📁 系统 (M)                        [编辑] [删除]   │ │
│  │   📁 用户管理 (M)                   [编辑] [删除]   │ │
│  │     📄 用户列表 (M)                  [编辑] [删除]  │ │
│  │       🔍 查询用户 (B)               [编辑] [删除]  │ │
│  │       ➕ 新增用户 (B)               [编辑] [删除]  │ │
│  │       ✏️ 编辑用户 (B)               [编辑] [删除]  │ │
│  │       🗑️ 删除用户 (B)               [编辑] [删除]  │ │
│  │   📁 角色管理 (M)                   [编辑] [删除]   │ │
│  │     📄 角色列表 (M)                  [编辑] [删除]  │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  图例：📁菜单(M) 📄页面(M) 🔍按钮(B)                    │
└─────────────────────────────────────────────────────────┘
```

**表格/树形展示切换：**

支持两种视图：
1. **树形视图**（默认）：层级展示权限关系
2. **表格视图**：列表展示所有权限，支持分页

#### 2.4.3 权限新增/编辑表单设计

**表单字段：**

| 字段名称 | 字段类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| 权限类型 | 单选 | 是 | 菜单/按钮 | - | M=菜单，B=按钮 |
| 上级权限 | 树形选择 | 否 | 必须选择已有的菜单 | - | 构建权限树 |
| 权限名称 | 文本输入 | 是 | 2-50字符 | - | 显示名称 |
| 权限标识 | 文本输入 | 是 | 若依风格：module:function:action | - | 如：system:user:add |
| 排序权重 | 数字输入 | 是 | 0-999 | 0 | 数字越小越靠前 |
| 菜单图标 | 图标选择 | 否 | - | - | 选择图标（菜单类型） |
| 路由路径 | 文本输入 | 菜单必填 | /开头 | - | 如：/system/user |
| 组件路径 | 文本输入 | 菜单必填 | - | - | 如：system/user/index |
| 是否显示 | 开关 | 是 | 显示/隐藏 | 显示 | 是否在菜单中显示 |
| 状态 | 单选 | 是 | 启用/禁用 | 启用 | 权限状态 |
| 描述 | 文本域 | 否 | 最多500字符 | - | 权限说明 |

**按钮权限的API关联配置：**

```
┌─────────────────────────────────────────┐
│  配置API权限                             │
├─────────────────────────────────────────┤
│                                         │
│  已关联的API（2个）：                     │
│  ┌───────────────────────────────────┐ │
│  │ API路径：/api/v1/users             │ │
│  │ 请求方法：☑GET ☐POST ☐PUT ☐DELETE  │ │
│  │ API说明：查询用户列表                │ │
│  │ [ 编辑 ] [ 删除 ]                   │ │
│  └───────────────────────────────────┘ │
│  ┌───────────────────────────────────┐ │
│  │ API路径：/api/v1/users/:id         │ │
│  │ 请求方法：☐GET ☐POST ☑PUT ☐DELETE  │ │
│  │ API说明：更新用户信息                │ │
│  │ [ 编辑 ] [ 删除 ]                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  [ + 添加API ]                          │
│                                         │
└─────────────────────────────────────────┘
```

**API添加表单：**

| 字段名称 | 字段类型 | 是否必填 | 说明 |
|---------|---------|---------|------|
| API路径 | 文本输入 | 是 | 如：/api/v1/users |
| 请求方法 | 多选框 | 是 | 可多选：GET/POST/PUT/DELETE |
| API说明 | 文本输入 | 否 | API功能说明 |

### 2.5 菜单管理模块详细设计

#### 2.5.1 功能清单

| 功能点 | 功能描述 | 优先级 | 权限标识 |
|-------|---------|--------|---------|
| **菜单列表** | 树形展示所有菜单 | P0 | system:menu:list |
| **菜单新增** | 添加新菜单 | P0 | system:menu:add |
| **菜单编辑** | 修改菜单信息 | P0 | system:menu:edit |
| **菜单删除** | 删除菜单（检查子菜单） | P0 | system:menu:delete |
| **菜单排序** | 拖拽调整菜单顺序 | P1 | system:menu:sort |
| **图标管理** | 管理菜单图标 | P2 | system:menu:icon |

#### 2.5.2 菜单管理界面设计

**界面布局：**

```
┌─────────────────────────────────────────────────────────┐
│  系统管理 > 菜单管理                                      │
├─────────────────────────────────────────────────────────┤
│  左侧：菜单树（可拖拽）           右侧：菜单详情/编辑     │
│  ┌───────────────────────┐  ┌───────────────────────┐ │
│  │ 📱 拖拽调整顺序         │  │ 菜单信息              │ │
│  │                       │  │ ─────────────────     │ │
│  │ 📁 系统管理            │  │ 菜单名称：系统管理     │ │
│  │   📁 用户管理          │  │ 菜单图标：⚙️          │ │
│  │     📄 用户列表        │  │ 路由路径：/system      │ │
│  │   📁 角色管理          │  │ 组件路径：Layout      │ │
│  │     📄 角色列表        │  │ 排序权重：1            │ │
│  │   📁 菜单管理          │  │ 是否显示：☑显示        │ │
│  │ 📁 业务管理            │  │ 菜单状态：启用         │ │
│  │   📁 内容管理          │  │                       │ │
│  │     📄 内容列表        │  │ [ 添加子菜单 ]         │ │
│  │                       │  │ [ 保存 ] [ 取消 ]      │ │
│  │ [ + 新增菜单 ]         │  │                       │ │
│  └───────────────────────┘  └───────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**交互说明：**

1. **拖拽排序**：支持拖拽菜单调整层级和顺序
2. **实时预览**：右侧实时显示选中菜单的详情
3. **快速编辑**：右侧支持直接编辑并保存
4. **批量操作**：支持批量修改菜单状态

### 2.6 部门管理模块详细设计

#### 2.6.1 功能清单

| 功能点 | 功能描述 | 优先级 | 权限标识 |
|-------|---------|--------|---------|
| **部门列表** | 树形展示所有部门 | P0 | system:dept:list |
| **部门新增** | 添加新部门 | P0 | system:dept:add |
| **部门编辑** | 修改部门信息 | P0 | system:dept:edit |
| **部门删除** | 删除部门（检查子部门和用户） | P0 | system:dept:delete |
| **部门排序** | 拖拽调整部门顺序 | P1 | system:dept:sort |
| **部门人员** | 查看部门成员 | P2 | system:dept:users |

#### 2.6.2 部门管理界面设计

**界面布局：**

```
┌─────────────────────────────────────────────────────────┐
│  系统管理 > 部门管理                                      │
├─────────────────────────────────────────────────────────┤
│  左侧：部门树（可拖拽）           右侧：部门详情/编辑     │
│  ┌───────────────────────┐  ┌───────────────────────┐ │
│  │ 📱 拖拽调整顺序         │  │ 部门信息              │ │
│  │                       │  │ ─────────────────     │ │
│  │ 🏢 XX公司              │  │ 部门名称：技术部       │ │
│  │   🏢 技术部            │  │ 部门编码：TECH        │ │
│  │     👥 研发组 (12人)   │  │ 负责人：张三           │ │
│  │     👥 测试组 (8人)    │  │ 联系电话：138xxx      │ │
│  │   🏢 产品部            │  │ 排序权重：1            │ │
│  │     👥 产品组 (5人)    │  │ 部门状态：启用         │ │
│  │   🏢 市场部            │  │                       │ │
│  │                       │  │ 部门人员：12人 [查看]  │ │
│  │ [ + 新增部门 ]         │  │                       │ │
│  └───────────────────────┘  │ [ 添加子部门 ]         │ │
│                             │ [ 保存 ] [ 取消 ]      │ │
│                             └───────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 2.7 租户管理模块详细设计

#### 2.7.1 功能清单

| 功能点 | 功能描述 | 优先级 | 权限标识 |
|-------|---------|--------|---------|
| **租户列表** | 展示所有租户信息 | P0 | system:tenant:list |
| **租户新增** | 创建新租户 | P0 | system:tenant:add |
| **租户编辑** | 修改租户信息 | P0 | system:tenant:edit |
| **租户删除** | 删除租户（检查用户） | P0 | system:tenant:delete |
| **配额管理** | 管理租户资源配额 | P1 | system:tenant:quota |
| **状态管理** | 启用/禁用租户 | P0 | system:tenant:edit |

#### 2.7.2 租户管理界面设计

**表格列：**

| 列名 | 说明 |
|-----|------|
| 租户名称 | 租户的公司/组织名称 |
| 租户编码 | 唯一标识（如：acme） |
| 联系人 | 主要联系人 |
| 联系电话 | 联系电话 |
| 用户数量 | 当前用户数 / 配额 |
| 存储空间 | 已使用 / 总配额 |
| 状态 | 启用/禁用/过期 |
| 创建时间 | 注册时间 |
| 过期时间 | 到期时间 |
| 操作 | 编辑、删除、配额管理 |

---

## 三、权限控制体系

### 3.1 RBAC权限模型

```
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│    用户       │ ──N:M──→│    角色       │ ──N:M──→│    权限       │
│  (User)      │         │   (Role)     │         │ (Permission) │
└──────────────┘         └──────────────┘         └──────────────┘
       │                       │                         │
       │                       │ ──继承──→               │
       │                       │                         │
       └──── user_roles ───→│                         │
                               │                         │
                               └── casbin_rule ───────→│
```

**模型说明：**

1. **用户-角色（N:M）**：一个用户可以拥有多个角色
2. **角色-权限（N:M）**：一个角色可以拥有多个权限
3. **角色继承**：子角色可以继承父角色的权限

### 3.2 四级权限体系

```
              ┌─────────────────┐
              │   数据权限（D）  │  ← 决定能看到哪些数据
              ├─────────────────┤
              │   API权限（A）   │  ← 决定能调用哪些接口
              ├─────────────────┤
              │   按钮权限（B）   │  ← 决定能看到哪些按钮
              ├─────────────────┤
              │   菜单权限（M）   │  ← 决定能看到哪些菜单
              └─────────────────┘
```

**权限层级关系：**

| 层级 | 权限类型 | 控制对象 | 生效位置 | 示例 |
|-----|---------|---------|---------|------|
| 1 | 菜单权限 | 前端菜单 | 前端 | 系统管理 > 用户管理 |
| 2 | 按钮权限 | 页面按钮 | 前端+后端 | 新增、编辑、删除按钮 |
| 3 | API权限 | 后端接口 | 后端 | GET /api/v1/users |
| 4 | 数据权限 | 数据范围 | 后端 | 仅本人、本部门数据 |

### 3.3 权限标识规范

采用**若依风格**：`模块:功能:操作`

**完整示例：**

```
system (系统管理模块)
├── system:user (用户管理模块)
│   ├── system:user:list (用户列表页面)
│   │   ├── system:user:query (查询按钮)
│   │   ├── system:user:add (新增按钮)
│   │   ├── system:user:edit (编辑按钮)
│   │   ├── system:user:delete (删除按钮)
│   │   ├── system:user:export (导出按钮)
│   │   └── system:user:import (导入按钮)
│   └── system:user:resetPwd (重置密码按钮)
├── system:role (角色管理模块)
│   ├── system:role:list (角色列表页面)
│   ├── system:role:add (新增角色)
│   ├── system:role:edit (编辑角色)
│   └── system:role:permission (权限配置)
└── system:menu (菜单管理模块)
    ├── system:menu:list (菜单列表)
    └── system:menu:sort (菜单排序)
```

**权限通配符：**

- `system:user:*` - 用户管理所有权限
- `system:*:list` - 所有列表页面权限
- `*:*:query` - 所有查询权限

### 3.4 预置角色体系

| 角色编码 | 角色名称 | 数据权限 | 说明 | 适用人员 |
|---------|---------|---------|------|---------|
| super_admin | 超级管理员 | 全部数据 | 平台级管理员，跨租户管理 | 系统管理员 |
| admin | 租户管理员 | 本租户全部数据 | 租户内的最高权限 | 企业IT管理员 |
| dept_manager | 部门经理 | 本部门及以下 | 管理部门内数据 | 部门负责人 |
| operator | 运营人员 | 自定义数据 | 跨部门协作 | 运营、编辑 |
| employee | 普通员工 | 仅本人数据 | 基础用户角色 | 普通员工 |
| auditor | 审计员 | 全部只读 | 只读审计权限 | 审计人员 |

---

## 四、认证与授权机制

### 4.1 JWT认证流程

#### 4.1.1 双令牌机制

**令牌类型：**

| 令牌类型 | 有效期 | 用途 | 存储位置 |
|---------|-------|------|---------|
| Access Token | 1小时 | 访问API接口 | localStorage |
| Refresh Token | 7天 | 刷新Access Token | localStorage |

**认证流程：**

```
┌──────────┐                ┌──────────┐
│  前端    │                │  后端    │
└────┬─────┘                └────┬─────┘
     │                           │
     │  1. 登录请求(user/pass)   │
     │─────────────────────────→│
     │                           │
     │              2. 验证密码  │
     │              查询角色权限  │
     │                           │
     │  3. 返回双Token           │
     │←─────────────────────────│
     │  {                       │
     │    access_token,         │
     │    refresh_token         │
     │  }                       │
     │                           │
     │  4. 携带access_token     │
     │  请求API                  │
     │─────────────────────────→│
     │                           │
     │              5. 验证Token │
     │              返回数据     │
     │←─────────────────────────│
     │                           │
     │  6. Token过期             │
     │  携带refresh_token        │
     │ 刷新Token                  │
     │─────────────────────────→│
     │                           │
     │              7. 生成新Token
     │←─────────────────────────│
```

#### 4.1.2 Token载荷内容

**Access Token包含信息：**

```json
{
  "tenant_id": "tenant_001",           // 租户ID
  "tenant_code": "acme",               // 租户编码
  "user_id": "user_001",               // 用户ID
  "user_name": "zhangsan",             // 用户名
  "roles": ["admin", "operator"],      // 角色列表
  "permissions": [                     // 权限标识列表
    "system:user:list",
    "system:user:add"
  ],
  "token_id": "sess_123456",           // 会话ID
  "exp": 1738080000,                   // 过期时间
  "iat": 1738076400                    // 签发时间
}
```

**Refresh Token包含信息：**

```json
{
  "user_id": "user_001",
  "token_id": "refresh_123456",
  "exp": 1738681200,                   // 7天后过期
  "iat": 1738076400
}
```

#### 4.1.3 Token黑名单机制

**黑名单使用场景：**

1. **用户主动登出**：将当前Token加入黑名单
2. **管理员强制下线**：将用户所有会话的Token加入黑名单
3. **密码修改后**：将该用户所有旧Token加入黑名单
4. **角色权限变更**：可选择是否让用户重新登录

**黑名单存储（Redis）：**

```
Key: blacklist:token:{token_id}
Value: {user_id}
TTL: Token剩余有效时间
```

### 4.2 Casbin授权机制

#### 4.2.1 Casbin模型配置

**模型选择：RBAC with Domains**

```
[request_definition]
r = sub, dom, obj, act

[policy_definition]
p = sub, dom, obj, act

[role_definition]
g = _, _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.sub, r.dom) && r.dom == p.dom && (r.obj == p.obj || p.obj == "*") && (r.act == p.act || r.act == "*")
```

#### 4.2.2 Casbin策略格式

**g策略（用户-角色关系）：**

```
g, user_001, admin, tenant_a
g, user_001, operator, tenant_a
```

**p策略（角色-权限关系）：**

```
# 菜单/按钮权限
p, admin, tenant_a, system:user:list, *
p, admin, tenant_a, system:user:add, *

# API权限
p, admin, tenant_a, /api/v1/users, GET
p, admin, tenant_a, /api/v1/users, POST
```

#### 4.2.3 权限验证流程

```
用户请求: GET /api/v1/users
  ↓
JWT中间件验证Token
  ↓
提取用户信息：user_id, tenant_code, roles
  ↓
Casbin验证权限
  ↓
获取用户角色：[admin, operator]
  ↓
检查角色是否有该API权限
  ↓
p, admin, tenant_a, /api/v1/users, GET ✓
  ↓
允许访问
```

**特殊角色处理：**

- **super_admin**：跳过Casbin验证，拥有所有权限
- **auditor**：只读权限，拒绝所有修改操作

---

## 五、数据权限设计

### 5.1 五级数据范围

| 级别 | 名称 | 代码 | SQL条件 | 适用角色 |
|-----|------|------|---------|---------|
| 1 | 全部数据权限 | DataScopeAll | 无额外WHERE | 超级管理员、租户管理员 |
| 2 | 自定义数据权限 | DataScopeCustom | `WHERE dept_id IN ('dept_001', 'dept_002')` | 跨部门管理者 |
| 3 | 本部门数据权限 | DataScopeDept | `WHERE dept_id = 'dept_001'` | 部门主管 |
| 4 | 本部门及以下 | DataScopeDeptChild | `WHERE dept_id IN (递归子部门)` | 事业群负责人 |
| 5 | 仅本人数据权限 | DataScopeSelf | `WHERE creator_id = 'user_001'` | 普通员工 |

### 5.2 数据权限应用场景

**场景1：用户管理**

| 角色 | 数据权限 | 能看到的数据 |
|-----|---------|-------------|
| 租户管理员 | 全部数据 | 所有用户 |
| 部门经理 | 本部门及以下 | 本部门及子部门的所有用户 |
| 普通员工 | 仅本人数据 | 只能看到自己的信息 |

**场景2：内容管理**

| 角色 | 数据权限 | 能看到的数据 |
|-----|---------|-------------|
| 运营人员 | 自定义数据 | 技术部、产品部的内容 |
| 编辑 | 仅本人数据 | 自己创建的内容 |

### 5.3 数据权限实现逻辑

**后端实现流程：**

```
1. 获取用户当前角色
   ↓
2. 查询角色的data_scope配置
   ↓
3. 如果用户有多个角色，取最大权限（data_scope最小值）
   ↓
4. 根据data_scope构建SQL条件
   ↓
5. 应用到数据库查询
   ↓
6. 返回过滤后的数据
```

---

## 六、前端展示方案

### 6.1 用户登录流程

```
用户打开登录页
  ↓
输入用户名、密码、验证码
  ↓
前端RSA加密密码
  ↓
调用登录API
  ↓
后端验证，返回双Token
  ↓
前端存储Token到localStorage
  ↓
请求用户菜单和权限API
  ↓
动态注册路由
  ↓
生成菜单和按钮
  ↓
跳转到首页
```

### 6.2 动态菜单渲染

**后端返回的菜单数据：**

```json
{
  "menus": [
    {
      "id": "menu_001",
      "name": "系统管理",
      "path": "/system",
      "icon": "Setting",
      "children": [
        {
          "id": "menu_002",
          "name": "用户管理",
          "path": "user",
          "icon": "User",
          "component": "system/user/index"
        }
      ]
    }
  ],
  "permissions": [
    "system:user:list",
    "system:user:add",
    "system:user:edit"
  ]
}
```

**前端渲染逻辑：**

1. 接收菜单数据，存储到Pinia/Vuex
2. 使用递归组件渲染菜单树
3. 点击菜单项，跳转到对应路由
4. 如果没有子菜单，直接跳转；如果有子菜单，展开/折叠

### 6.3 按钮权限控制

**方式1：自定义指令**

```vue
<el-button v-permission="['system:user:add']">新增</el-button>
<el-button v-permission="['system:user:edit']">编辑</el-button>
<el-button v-permission="['system:user:delete']">删除</el-button>
```

**方式2：函数式检查**

```vue
<el-button v-if="hasPermission('system:user:add')">新增</el-button>
```

### 6.4 前后端权限双重保障

```
前端权限控制（UI层）
├── 动态菜单（只显示有权限的菜单）
├── 动态路由（只注册有权限的路由）
└── 按钮隐藏（只显示有权限的按钮）
         ↓
后端权限控制（API层）
├── JWT认证（验证用户身份）
├── Casbin授权（验证用户权限）
└── 数据权限过滤（过滤数据范围）
```

**重要说明：**

- 前端权限只是为了**用户体验**（不显示无权内容）
- 后端权限才是**真正的安全保障**（即使绕过前端也会被拦截）
- 前后端权限配置必须**一致**

---

## 七、后台管理功能

### 7.1 用户管理功能

**页面路径：** `/system/users`

**主要操作：**

1. **查询用户**：支持多条件搜索
2. **新增用户**：填写表单，分配角色
3. **编辑用户**：修改用户信息和角色
4. **删除用户**：软删除，检查关联
5. **重置密码**：强制重置为默认密码
6. **启用/禁用**：控制用户登录状态
7. **查看权限**：弹窗展示用户的所有权限
8. **导入导出**：Excel批量操作

### 7.2 角色管理功能

**页面路径：** `/system/roles`

**主要操作：**

1. **查询角色**：搜索角色
2. **新增角色**：填写基本信息
3. **编辑角色**：修改角色信息
4. **删除角色**：检查是否有用户使用
5. **权限配置**：勾选权限树，自动关联API
6. **数据权限配置**：选择数据范围
7. **角色复制**：快速创建相似角色
8. **角色继承**：设置父角色

### 7.3 权限管理功能

**页面路径：** `/system/permissions`

**主要操作：**

1. **查询权限**：树形展示，支持搜索
2. **新增权限**：添加菜单/按钮权限
3. **编辑权限**：修改权限信息和API关联
4. **删除权限**：检查是否被角色使用
5. **API关联**：为按钮/菜单关联API
6. **导入导出**：Excel/JSON格式

### 7.4 菜单管理功能

**页面路径：** `/system/menus`

**主要操作：**

1. **查询菜单**：树形展示
2. **新增菜单**：添加菜单/页面
3. **编辑菜单**：修改菜单信息
4. **删除菜单**：检查子菜单
5. **拖拽排序**：调整菜单顺序和层级
6. **图标选择**：选择菜单图标

### 7.5 部门管理功能

**页面路径：** `/system/departments`

**主要操作：**

1. **查询部门**：树形展示
2. **新增部门**：添加部门
3. **编辑部门**：修改部门信息
4. **删除部门**：检查子部门和用户
5. **拖拽排序**：调整部门顺序
6. **查看人员**：查看部门成员

### 7.6 在线用户管理

**页面路径：** `/monitor/online`

**主要操作：**

1. **查看在线用户**：显示当前登录的用户
2. **强退用户**：强制用户下线（Token加入黑名单）
3. **查看会话详情**：显示登录IP、地点、浏览器等

### 7.7 操作日志

**页面路径：** `/monitor/operlog`

**主要操作：**

1. **查询日志**：支持多条件搜索
2. **查看详情**：查看操作详细信息
3. **导出日志**：导出为Excel

**记录的操作：**

- 用户新增/修改/删除
- 角色新增/修改/删除
- 权限变更
- 密码修改
- 登录/登出

---

## 八、用户操作流程

### 8.1 管理员操作流程

#### 流程1：为新员工开通账号

```
开始
  ↓
【用户管理】→ 点击「新增用户」
  ↓
填写用户信息：
- 用户名（必填，唯一）
- 初始密码（必填）
- 姓名（必填）
- 手机号（必填）
- 部门（必填，树形选择）
- 角色（必填，可多选）
  ↓
点击「确定」
  ↓
系统自动：
✓ 创建用户账号
✓ 分配角色
✓ 继承角色权限
✓ 记录操作日志
  ↓
通知用户：
- 用户名
- 初始密码
- 登录地址
  ↓
完成
```

#### 流程2：为角色配置权限

```
开始
  ↓
【角色管理】→ 选择角色 → 点击「权限配置」
  ↓
展开权限树，勾选权限：
☑ 系统管理
  ├─ ☑ 用户管理
  │    ├─ ☑ 用户查询
  │    ├─ ☑ 新增用户
  │    ├─ ☑ 编辑用户
  │    └─ ☐ 删除用户
  └─ ☑ 角色管理
  ↓
点击「保存」
  ↓
系统自动：
✓ 清除角色旧权限
✓ 添加新权限到Casbin
✓ 自动关联API权限
✓ 记录操作日志
✓ 权限实时生效
  ↓
完成
```

### 8.2 普通用户操作流程

#### 流程1：用户登录

```
打开登录页面
  ↓
输入：用户名 + 密码 + 验证码
  ↓
点击「登录」
  ↓
前端RSA加密密码
  ↓
调用登录API
  ↓
后端验证，返回Token
  ↓
前端存储Token
  ↓
请求菜单和权限API
  ↓
动态生成菜单和路由
  ↓
跳转到首页
```

#### 流程2：修改密码

```
点击头像 → 「个人中心」
  ↓
点击「修改密码」
  ↓
输入：
- 原密码
- 新密码
- 确认新密码
  ↓
点击「确定」
  ↓
后端验证原密码
  ↓
更新新密码
  ↓
将所有Token加入黑名单
  ↓
强制重新登录
```

---

## 九、典型应用场景

### 9.1 场景1：新员工入职

**场景描述：**
公司新入职一名「技术部 - 研发工程师」，需要开通系统账号。

**操作流程：**

1. 管理员进入「用户管理」→「新增用户」
2. 填写用户信息：
   - 用户名：`lisi`
   - 姓名：`李四`
   - 部门：技术部 > 研发组
   - 角色：普通员工
3. 保存后，系统自动分配权限
4. 通知用户账号信息

**权限效果：**
- 菜单权限：可访问基础功能模块
- 按钮权限：查看、编辑自己创建的数据
- 数据权限：仅本人数据

### 9.2 场景2：跨部门协作

**场景描述：**
市场部的「张三」需要查看技术部的项目进度，但不允许编辑。

**操作流程：**

1. 创建新角色：「项目查看员」
2. 配置权限：
   - 菜单权限：项目管理 > 项目列表（查看）
   - 按钮权限：❌新增、❌编辑、❌删除
   - API权限：✅GET /api/v1/projects（查询）
   - 数据权限：自定义（技术部、产品部）

**权限效果：**
- 张三可以看到项目管理菜单
- 「新增」、「编辑」、「删除」按钮不显示
- 前端尝试调用编辑接口会被后端拦截
- 只能查看技术部和产品部的项目数据

### 9.3 场景3：临时权限授予

**场景描述：**
普通员工「王五」需要临时导出一份报表。

**解决方案：**

**方案1：临时添加角色**
1. 管理员为「王五」添加「报表导出员」角色
2. 角色配置：
   - 菜单权限：数据报表模块
   - 按钮权限：导出报表
   - API权限：POST /api/v1/reports/export
   - 数据权限：自定义（只允许导出本部门数据）
3. 王五导出报表后，管理员移除该角色

**方案2：临时权限申请（推荐）**
1. 王五提交「权限申请」
2. 填写申请信息：
   - 申请权限：数据报表导出
   - 申请时长：2小时
   - 申请理由：需要导出月度销售报表
3. 直属领导审批
4. 审批通过后，系统自动：
   - 为王五添加临时角色（设置过期时间）
   - 2小时后自动移除角色
   - 记录完整的申请和审批日志

### 9.4 场景4：离职人员权限回收

**场景描述：**
员工「赵六」离职，需要立即回收所有权限。

**操作流程：**

1. 管理员进入「用户管理」
2. 找到「赵六」，点击「编辑」
3. 将状态改为「禁用」
4. 保存

**系统自动处理：**
- ✓ 将赵六的所有Token加入黑名单
- ✓ 立即强制下线
- ✓ 禁止登录
- ✓ 记录操作日志

---

## 十、安全机制设计

### 10.1 密码安全

| 安全措施 | 实现方式 |
|---------|---------|
| 传输加密 | RSA公钥加密密码 |
| 存储加密 | Argon2id + SHA256 |
| 密码复杂度 | 6-20位，包含字母和数字 |
| 密码过期 | 可配置策略（如90天） |
| 密码历史 | 禁止使用最近5次使用过的密码 |
| 重置密码 | 管理员强制重置/邮箱自助重置 |

### 10.2 Token安全

| 安全措施 | 实现方式 |
|---------|---------|
| 双令牌机制 | Access Token（1小时）+ Refresh Token（7天） |
| Token黑名单 | Redis存储，登出时立即失效 |
| Token刷新 | Access Token过期前5分钟自动刷新 |
| 会话管理 | 支持单设备/多设备登录 |
| 强制下线 | 管理员可强制指定用户下线 |

### 10.3 接口安全

| 安全措施 | 实现方式 |
|---------|---------|
| HTTPS加密 | TLS 1.3 |
| CORS限制 | 白名单配置 |
| 请求限流 | 滑动窗口算法，每分钟最多60次请求 |
| SQL注入防护 | 参数化查询 |
| XSS防护 | 前端输出HTML时自动转义 |

### 10.4 数据安全

| 安全措施 | 实现方式 |
|---------|---------|
| 租户隔离 | 所有查询自动添加租户过滤 |
| 软删除 | 数据不物理删除，保留deleted_at字段 |
| 敏感数据加密 | 手机号、身份证等加密存储 |
| 操作审计 | 完整日志记录 |
| 数据备份 | 每日自动备份 |

### 10.5 安全审计

**审计内容：**

1. **登录审计：**
   - 登录成功/失败记录
   - 登录IP、地点、时间
   - 异常登录检测

2. **操作审计：**
   - 新增/修改/删除操作
   - 敏感数据查询
   - 权限变更记录

3. **数据审计：**
   - 数据变更历史
   - 敏感字段变更前后对比
   - 数据导出记录

---

## 十一、产品交互细节

### 11.1 消息提示规范

| 提示类型 | 使用场景 | 显示位置 | 消失方式 |
|---------|---------|---------|---------|
| 成功提示 | 操作成功 | 顶部右上角 | 3秒后自动消失 |
| 错误提示 | 操作失败 | 顶部右上角 | 需手动关闭 |
| 警告提示 | 危险操作确认 | 屏幕中央 | 需用户确认 |
| 信息提示 | 一般提示 | 顶部右上角 | 3秒后自动消失 |

**示例：**

- ✅ 成功：「用户创建成功」
- ❌ 错误：「用户名已存在，请更换」
- ⚠️ 警告：「确定要删除该用户吗？此操作不可恢复」
- ℹ️ 信息：「已为您保存了草稿」

### 11.2 表单验证规范

**验证时机：**

1. **失焦验证**：输入框失焦时立即验证
2. **提交验证**：点击提交按钮时验证所有字段
3. **实时验证**：用户名、邮箱等需要异步检查唯一性

**错误提示：**

- 错误字段标红
- 错误提示显示在字段下方
- 提交时滚动到第一个错误字段

### 11.3 加载状态规范

**加载场景：**

1. **页面加载**：显示全屏加载动画
2. **按钮加载**：按钮显示loading状态，禁用按钮
3. **表格加载**：显示骨架屏
4. **数据加载**：显示loading图标

### 11.4 确认操作规范

**需要确认的操作：**

- 删除操作（单个删除、批量删除）
- 禁用用户
- 强退用户
- 敏感数据修改

**确认弹窗内容：**

- 操作说明
- 影响范围（如：将删除1个用户）
- 后果提醒（如：此操作不可恢复）
- 确认/取消按钮

---

## 十二、扩展功能规划

### 12.1 权限申请与审批

**功能描述：**
普通用户可以申请需要的权限，由上级或管理员审批。

**流程：**

```
用户提交申请
  ↓
填写申请信息：
- 申请权限
- 申请时长
- 申请理由
  ↓
提交审批
  ↓
审批人收到通知
  ↓
审批人操作：
- 通过：自动添加临时角色，到期自动移除
- 拒绝：记录拒绝理由
  ↓
通知申请人
```

### 12.2 权限模板

**预置模板：**

| 模板名称 | 适用场景 | 包含权限 |
|---------|---------|---------|
| 基础用户模板 | 新员工默认权限 | 个人中心、工作台 |
| 管理员模板 | 部门管理员 | 用户管理、角色管理、部门管理 |
| 运营人员模板 | 运营专员 | 内容管理、数据分析 |
| 只读审计模板 | 审计员 | 所有模块只读权限 |

### 12.3 国际化支持

**多语言支持：**

- 系统支持中英文切换
- 菜单名称、角色名称支持多语言
- 权限标识保持统一（英文）

### 12.4 移动端适配

**响应式设计：**

- 支持手机、平板访问
- 菜单自动折叠为汉堡菜单
- 表格支持横向滚动
- 按钮适配触摸操作

---

## 十三、总结

### 13.1 核心优势

1. **多租户隔离**：确保不同租户的数据和权限完全隔离
2. **四级权限控制**：菜单、按钮、API、数据四级精细化权限控制
3. **若依风格**：采用行业标准，便于理解和维护
4. **灵活授权**：通过角色聚合权限，支持角色继承
5. **安全可靠**：多重安全机制，确保系统和数据安全
6. **易于扩展**：统一权限管理，便于功能扩展和维护

### 13.2 适用场景

- 企业内部管理系统（OA、ERP、CRM）
- SaaS多租户平台
- 需要精细化权限控制的中大型应用
- 多部门、多层级的企业组织架构

### 13.3 技术亮点

- **双Token机制**：Access Token + Refresh Token
- **Casbin权限引擎**：强大的访问控制库
- **数据权限**：5级数据范围控制
- **前后端双重保障**：前端UI控制 + 后端API验证
- **权限标识规范**：若依风格（模块:功能:操作）

---

**文档版本：** v2.0 Complete
**创建日期：** 2026-01-27
**文档类型：** 产品设计文档（纯产品设计，无代码实现）
**维护人：** 产品团队
**审核状态：** 待审核

---

## 参考资源

本文档参考了以下业界最佳实践：

- [若依框架官方文档](http://doc.ruoyi.vip/)
- [Casbin官方文档](https://casbin.org/docs/zh-CN/overview)
- [SaaS多租户数据权限设计（参考RuoYi）](https://www.cnblogs.com/lucky_hu/p/18494138)
- [权限系统设计详解 - JavaGuide](https://javaguide.cn/system-design/security/design-of-authority-system.html)
- [万字长文：深入浅出RBAC权限设计](https://www.woshipm.com/pd/5576757.html)
- [前端权限开发——设计到实践](https://www.cnblogs.com/sunupo/p/18309967)
- [如何高效实现前端权限管理？从RBAC到动态路由实践](https://blog.csdn.net/mmc123125/article/details/145520161)
